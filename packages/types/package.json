{
  "name": "@galatabaker/types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared TypeScript types",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}

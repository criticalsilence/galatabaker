# apps/web — public env vars (safe to commit)
# Copy to .env.local and fill in real values for development.

# API base URL (NestJS backend). Must include /api prefix.
# Default for local dev: http://localhost:3001/api
NEXT_PUBLIC_API_URL=http://localhost:3001/api
